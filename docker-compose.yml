version: '3'
services:
  app:
    build: Solver
    volumes: 
      - app:/app
    ports:
      - '5000:5000'

  proxy:
    build: nginx
    restart: always
    ports:
      - 80:80
    depends_on:
      - app
volumes:
  app: 